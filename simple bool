#include <iostream>
#include <string>
using namespace std;

int main()
{
    bool more = true;
    while (more)
    {
        cout << "More data? (y/n) ";

        //string answer;
        //getline(cin, answer);
        //if(answer != "y")
        //{
        //  more = false;
        //}

        int answer;
        cin >> answer;

        if(answer != 1)
        {
            more = false;
        }
    }
    return 0;
}
